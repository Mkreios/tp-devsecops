@model WebApplicationNavigation.Models.Matiere

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>
<table class="table">
    <tr>
        <th>Nom</th>
        <th>prénom</th>
        <th>Date De Naissance</th>
        <th>Groupe</th>
    </tr>
    @foreach (var item in Model.Inscriptions)
    {
        <tr>
            <td>@Html.DisplayFor(x => item.Etudiant.Name)</td>
            <td>@Html.DisplayFor(x => item.Etudiant.LastName)</td>
            <td>@Html.DisplayFor(x => item.Etudiant.DateN)</td>
            <td>@Html.DisplayFor(x => item.Etudiant.Groupe.LibGroupe)</td>
        </tr>
    }
</table>
<div>
    <h4>Matiere</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.libMatiere)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.libMatiere)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
